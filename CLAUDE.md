## Agent skills

### Issue tracker

Issues live in GitHub Issues (github.com/arthurbre/bluealsa2sendspin), via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout (root `CONTEXT.md` + `docs/adr/`). See `docs/agents/domain.md`.
